@model IEnumerable<QLBHDTDD.Models.Danhmuc_sp>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh Mục</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_danhmuc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_danhmuc)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.ID_dm }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.ID_dm }) |
            @Html.ActionLink("Xoá", "Delete", new { id=item.ID_dm })
        </td>
    </tr>
}

</table>
